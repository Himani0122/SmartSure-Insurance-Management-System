spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:5432/auth_db
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:password}

# ============================================================
# SMTP / Email Configuration (Gmail)
# STEP 1: Replace with YOUR gmail address
# STEP 2: Replace with YOUR 16-char App Password (no spaces)
#         Get it at: myaccount.google.com → Security → App Passwords
#         (Requires 2-Step Verification to be enabled first)
# ============================================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=singhhimani623@gmail.com
spring.mail.password=hgwtimytymowixse
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=15000
spring.mail.properties.mail.smtp.timeout=15000
spring.mail.properties.mail.smtp.writetimeout=15000
